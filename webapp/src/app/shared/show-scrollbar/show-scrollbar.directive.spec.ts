import { ShowScrollbarDirective } from './show-scrollbar.directive';

describe('ShowScrollbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowScrollbarDirective();
    expect(directive).toBeTruthy();
  });
});
